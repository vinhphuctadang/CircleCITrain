# This compose file is only for testing purpose
version: '3.2'
services:
  database:
    image: 'mongo:latest'
    container_name: mongo
    ports:
      - '27017:27017'
  app:
    image: 'node:8.17'
    container_name: app
    environment:
    - ENV=test
